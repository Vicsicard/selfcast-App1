# Supabase Configuration
# ------------------
# Your Supabase project URL (find in Project Settings -> API)
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (find in Project Settings -> API)
# IMPORTANT: Use the anon/public key, NOT the service_role key
SUPABASE_KEY=your-anon-key

# OpenAI Configuration
# ------------------
# Your OpenAI API key for generating embeddings
OPENAI_API_KEY=your-openai-key

# Optional Configuration
# ------------------
# Default project ID (can be overridden via --project-id)
#PROJECT_ID=default-project

# Default user ID (can be overridden via --user-id)
#USER_ID=default-user

# To use this file:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Uncomment any optional variables you want to use
# 4. Keep .env in .gitignore to protect your credentials
